# MCP SSH tmux

An MCP server that manages SSH sessions using a local `tmux` instance. 

## Key Features
- **Persistence**: Sessions stay alive if the MCP server restarts.
- **Observability**: Attach to the `tmux` session to see what the agent is doing.
- **Simplicity**: No complex internal buffer management.
